<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Attak Tracker</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
  <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.0/font/bootstrap-icons.css"
    />
</head>
<body>
  
  {{#if logged_in}}
    <span
      class="absolute text-white text-4xl top-5 left-4 cursor-pointer pl-2"
      onclick="openSidebar()"
    >
      <i class="bi bi-filter-left px-2 bg-gray-900 rounded-md icon-yellow"></i>
    </span>
    <div
      class="sidebar fixed top-0 bottom-0 lg:left-0 p-2 w-[300px] overflow-y-auto text-center bg-gray-900  hidden"
    >
      <div class="text-gray-100 text-xl">
        <div class="p-2.5 mt-1 flex items-center">
          <h1 class="font-bold text-yellow text-[15px] ml-3">Attak Tracker</h1>
          <i
            class="bi bi-x cursor-pointer ml-28 icon-yellow"
            onclick="openSidebar()"
          ></i>
        </div>
        <div class="my-2 bg-gray-600 h-[1px]"></div>
      </div>
      <div
        class="p-2.5 mt-3 flex items-center rounded-md px-4 duration-300 cursor-pointer hover:bg-gray-600 text-white"
      >
        <i class="bi bi-chevron-compact-right icon-yellow"></i>
        
        <span class="text-[15px] ml-4 text-yellow font-bold">
          <a href="/">
          Homepage</a></span>
      </div>
      <div
        class="p-2.5 mt-3 flex items-center rounded-md px-4 duration-300 cursor-pointer hover:bg-gray-600 text-white">
        <i class="bi bi-chevron-compact-right icon-yellow"></i>
        <span class="text-[15px] ml-4 text-yellow font-bold">
          <a href="/exercise">
          Exercises</a></span>
      </div>
      <div class="my-4 bg-gray-600 h-[1px]"></div>
      <div
        class="p-2.5 mt-3 flex items-center rounded-md px-4 duration-300 cursor-pointer hover:bg-gray-600 text-white">
        <i class="bi bi-chevron-compact-right icon-yellow"></i>
        <div class="flex justify-between w-full items-center">
          <span class="text-[15px] ml-4 text-yellow font-bold"><a href="/workout">
            Workouts</a></span>
        </div>
      </div>
      <div
        class="p-2.5 mt-3 flex items-center rounded-md px-4 duration-300 cursor-pointer hover:bg-gray-600 text-white"
      >
        <i class="bi bi-box-arrow-in-right icon-yellow"></i>
        <span 
       id="logout" class="text-[15px] ml-4 text-yellow font-bold">
          
          Logout</span>
      </div>
    </div>
      {{/if}}

    <main class="container container-fluid mt-2">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
   <script type="text/javascript">
      
      function openSidebar() {
        document.querySelector(".sidebar").classList.toggle("hidden");
      }
    </script>
  {{/if}}

</body>

</html>
